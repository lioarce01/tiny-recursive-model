# Core dependencies
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# Data processing
numpy>=1.21.0
pandas>=1.3.0
# arckit>=0.1.0  # Optional: For ARC dataset loading (install manually if needed)

# Utilities
tqdm>=4.62.0
requests>=2.25.0

# Optional: for experiment tracking (uncomment if needed)
# wandb>=0.12.0
# tensorboard>=2.8.0

# Development
pytest>=6.2.0
black>=21.0.0
flake8>=3.9.0